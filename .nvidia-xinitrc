#!/bin/bash

# used in .xinitrc as a hint to load nv stuffs
export NVIDIA=1

# load nv vk stuffs instead of intel
export VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/nvidia_icd.json

# force scaling = 1
export WINIT_HIDPI_FACTOR=1.0
export QT_DEVICE_PIXEL_RATIO=1

sh .xinitrc
