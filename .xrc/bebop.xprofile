#!/bin/sh
#
# environemnt variables which are defined at the DM level for bebop

# for alacritty
#export WINIT_HIDPI_FACTOR=2.0

# i3 background
export FEH_BG=$HOME/Pictures/Pictures/ScreenSaver/celeste/complete-8.png

if [ "$XDG_CURRENT_DESKTOP" = "i3" ]; then
    xrandr --output DP-1-1-8 --auto --primary --output DP-1-1-1-8 --right-of DP-1-1-8 --auto --output eDP-1 --off
fi
