" jshap's gvimrc
" because apparently configuring vim is just that hard

" beep and flash, begone.
set visualbell t_vb=

" set mouse model to allow for right-click dialogs
set mousemodel=popup_setpos

" set cursor color to same as term
" guifg=#002b36
" note that this doesn't work and is overwritten by my theme :(
highlight Cursor guibg=#ffaf00
