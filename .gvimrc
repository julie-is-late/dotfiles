" jshap's gvimrc
" because apparently configuring vim is just that hard

" beep and flash, begone.
set visualbell t_vb=

" set mouse model to allow for right-click dialogs
set mousemodel=popup_setpos

